{
  "mcpServers": {
    "haivemind": {
      "command": "npx",
      "args": ["@modelcontextprotocol/server-http", "http://localhost:8900/sse"],
      "env": {
        "HTTP_TIMEOUT": "60000",
        "RETRY_DELAY": "1000",
        "MAX_RETRIES": "5",
        "KEEP_ALIVE": "true"
      }
    }
  }
}