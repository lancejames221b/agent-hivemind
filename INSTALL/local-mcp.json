{
  "_instructions": {
    "_comment": "Memory MCP Server - Local Configuration (for lance-dev machine)",
    "_installation": [
      "1. This configuration is for the local machine running the memory server",
      "2. Remove the '_instructions' section from the JSON", 
      "3. Place the cleaned JSON file as .mcp.json in your project directory",
      "4. OR merge the mcpServers section into your existing .mcp.json",
      "5. The local memory server runs via stdio (not HTTP)",
      "6. Services are managed by systemctl: sudo systemctl status memory-sync"
    ],
    "_services": {
      "memory_mcp": "systemctl status memory-mcp (stdio MCP server)",
      "memory_sync": "systemctl status memory-sync (HTTP sync service on port 8899)"
    },
    "_memory_tools": [
      "store_memory - Store memories with categories (project, conversation, agent, global)",
      "search_memories - Semantic search across all memories", 
      "retrieve_memory - Get specific memory by ID",
      "get_recent_memories - Get recent memories by time window",
      "get_memory_stats - View memory collection statistics"
    ]
  },
  "mcpServers": {
    "memory-server": {
      "command": "python3",
      "args": ["/home/lj/memory-mcp/src/memory_server.py"],
      "env": {
        "PYTHONPATH": "/home/lj/memory-mcp/src"
      }
    }
  }
}